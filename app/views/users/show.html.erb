<h1><%= @user.name %>のページ</h1>

<h1>メールアドレス: <%= @user.email %></h1>

<h1>ユーザーアイコン画像: <%= image_tag(@user.image.url) if @user.image.url %>

<h1 class="content">お気に入りツイート一覧</h1>
　<% @favorites_twiclos.each do |twiclo| %>
    <li><%= link_to "#{twiclo.content}", twiclo_path(twiclo.id) %></li>
  <% end %>